# Manual mirror of pyproject.toml deps (runtime [project] list + dev tooling from [dependency-groups]), kept for the Dockerfile and CI installs — update BOTH files when deps change (local dev uses the uv workspace instead).
google-adk[otel-gcp]>=1.27.2
google-genai>=1.68.0
pydantic>=2.0.0
python-dotenv
mcp[sse]
a2a-sdk>=0.3.25
uvicorn
fastapi
httpx
gunicorn
pytest
pytest-asyncio
click
ruff
mypy
redis>=5.0.0
asyncpg>=0.30.0
pgvector>=0.3.0
-e ../nexus-common
